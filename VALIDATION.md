# 1.0.0 검증 범위

2026-09-12, Windows / Python 3.11.5.

- 로컬 도구 6개 테스트 통과: 렌더 캐시 재사용·손상 복구, 범위 오류 거부, 상태/리비전 충돌과 실패 기록 보존, 경로 이탈 거부, 해시가 유효해도 원본과 다른 크롭 픽셀 탐지.
- 격리 작업 폴더에 venv 및 의존성 신규 설치, 동명 스킬 업데이트와 백업, 기존 설정 유지 확인.
- 실제 TeamsWord MCP에 창작 예시 4개 등록 후 구조 재조회. 지도·연표·편지는 이미지, 일반 보기는 텍스트·밑줄로 보존.
- 이미지 3개를 asset_read로 다시 읽어 로컬 크롭과 바이트 동일성 확인.
- 오래된 expectedVersion 쓰기 dryRun은 DOCUMENT_VERSION_CONFLICT로 거부. 잘못된 이미지 바이트는 UNSUPPORTED_IMAGE_TYPE으로 거부. 검증 문항은 4개로 유지.
- 스킬 기본 형식 검사 통과.

미완료: 실제 편집기 화면 검증(iab 도구 없음), 새 턴의 스킬 인식, GitHub 주소 기반 설치, 운영 시험지 대표 문항의 종단 검증. 따라서 현재 등록 표본은 registered이며 verified가 아니다. 창작 표본은 실제 시험지 추출 정확도를 입증하지 않는다.

숫자 배점 설정과 공유 지문 연결은 현재 MCP 지원을 별도로 확인해야 한다. 로컬 스크립트는 원문 판독이나 의미 정확도를 자동 채점하지 않는다.
