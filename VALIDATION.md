# 1.0.1 검증 범위

2026-09-12, Windows / Python 3.11.5. 1.0.1 은 문서 변경만 있으며 Python 도구는 1.0.0 과 같다(테스트 6개 재실행 통과).

## 로컬 도구

- 테스트 6개 통과: 렌더 캐시 재사용·손상 복구, 범위 오류 거부, 상태/리비전 충돌과 실패 기록 보존, 경로 이탈 거부, 해시가 유효해도 원본과 다른 크롭 픽셀 탐지.
- 격리 작업 폴더에 venv 및 의존성 신규 설치, 동명 스킬 업데이트와 백업, 기존 설정 유지 확인.
- 스킬 기본 형식 검사 통과.

## 실제 TeamsWord MCP 등록

| 표본 | 범위 | 저장 구조 재조회 | 실제 편집기 화면 | 최종 상태 |
|---|---|---|---|---|
| 창작 예시 4종 | 지도·텍스트 보기·연표·편지 | 통과, 이미지 3개 asset_read 바이트 동일 | 미확인(iab 없음) | registered |
| 2026 수능 한국사 홀수형 1~5 | 이미지 자료 4, 텍스트 보기 1 | 통과, 이미지 4개 바이트 동일, 정답 5개 | Chrome 대조 통과(보기 기본 제목 제거 1건) | 1·2·4 verified, 3·5 registered |
| 2026 수능 국어 홀수형 1~3 + 공통 지문 | 텍스트만, 이미지 없음 | 통과, 정답 3개, 테두리·밑줄 확인 | Chrome 대조 통과 | 1·2 verified, 3 registered |

- 오래된 expectedVersion 쓰기 dryRun 은 DOCUMENT_VERSION_CONFLICT 로 거부. 잘못된 이미지 바이트는 UNSUPPORTED_IMAGE_TYPE 으로 거부.
- 국어 공통 지문은 사용자 지정에 따라 별도 `general_document` 로 분리했고 1~3번 본문에서 제거했다. 문항은 지문 문서 ID 를 기록으로만 참조하며 TeamsWord 의 네이티브 공유 연결은 아니다.
- 3·5번(한국사)·3번(국어)이 registered 에 머문 이유는 하나다: `[3점]` 숫자 배점을 `item.scoring.set` 이 받지 않는다(checkType·maxChoices 만). 인쇄 표시는 본문에 보존했다.
- 시간 실측: 한국사 5문항 413.1초, 국어 3문항+지문 632.5초(분리 전 이력). 단일 실측이며 일반화하지 않는다.

실제 시험지·크롭·MCP 응답·화면 관찰 기록은 작업 폴더(`runs/history-measure`, `runs/korean-measure`)에 있고 배포에 포함하지 않는다.

## 미완료

- GitHub 주소 기반 설치 재현, 새 턴의 스킬 인식 확인.
- 숫자 배점 설정(MCP 미지원), 공유 지문 네이티브 연결(MCP 미지원).
- 독립적인 사람 검수·블라인드 평가 없음. 이미지 크롭 정확도 근거는 한국사 표본 4개뿐이다. 국어 표본은 이미지가 없어 크롭을 평가하지 않는다.
- 국어 실행은 정식 question 스키마 검사를 등록 뒤에 집계했다. 등록 전 검사 순서 준수는 다음 실행에서 확인해야 한다.
- 창작 표본과 두 과목 표본은 실제 시험지 전체의 추출 정확도를 입증하지 않는다.
